apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: marketintel
  labels: {app: marketintel, component: web}
spec:
  type: ClusterIP
  selector: {app: marketintel, component: web}
  ports:
    - {name: http, port: 80, targetPort: 8000}
